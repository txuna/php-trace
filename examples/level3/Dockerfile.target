FROM ubuntu:24.04

ENV DEBIAN_FRONTEND=noninteractive

RUN apt-get update && apt-get install -y gcc && rm -rf /var/lib/apt/lists/*

WORKDIR /app
COPY target.c .
# -O0: 인라인 방지, -g: DWARF 심볼 보존
RUN gcc -O0 -g -o target target.c
